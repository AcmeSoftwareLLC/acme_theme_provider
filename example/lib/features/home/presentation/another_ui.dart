import 'package:flutter/cupertino.dart';

class AnotherUI extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is another UI'),
    );
  }
}