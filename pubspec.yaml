name: acme_theme_provider
description: A theme provider for themes generated using Acme Theme Components.
version: 0.2.0
homepage: https://acmesoftware.com

environment:
  sdk: '>=2.17.0 <3.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4
  json_theme:
    git:
      url: https://github.com/sarbagyastha/json_theme.git

dev_dependencies:
  flutter_test:
    sdk: flutter